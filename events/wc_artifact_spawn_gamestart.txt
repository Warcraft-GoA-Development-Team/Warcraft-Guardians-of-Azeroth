namespace = WCARTSPWN

character_event = {
	id = WCARTSPWN.1

	is_triggered_only = yes
	hide_window = yes

	ai = no

	trigger = {
		is_save_game = no
		OR = {
			multiplayer = no
			is_multiplayer_host_character = yes
		}
		NOT = {
			has_game_rule = {
				name = artifact_spawn
				value = off
			}
		}
	}

	immediate = {
		game_start_spawn_artifacts_random_effect = yes
		if = {
			limit = {
				has_game_rule = {
					name = artifact_spawn
					value = x0_5
				}
			}
			game_start_spawn_artifacts_random_effect = yes
		}
		if = {
			limit = {
				has_game_rule = {
					name = artifact_spawn
					value = default
				}
			}
			game_start_spawn_artifacts_random_effect = yes
			game_start_spawn_artifacts_random_effect = yes
			game_start_spawn_artifacts_random_effect = yes
		}
		if = {
			limit = {
				has_game_rule = {
					name = artifact_spawn
					value = x1_5
				}
			}
			game_start_spawn_artifacts_random_effect = yes
			game_start_spawn_artifacts_random_effect = yes
			game_start_spawn_artifacts_random_effect = yes
			game_start_spawn_artifacts_random_effect = yes
			game_start_spawn_artifacts_random_effect = yes
		}
		if = {
			limit = {
				has_game_rule = {
					name = artifact_spawn
					value = x2
				}
			}
			game_start_spawn_artifacts_random_effect = yes
			game_start_spawn_artifacts_random_effect = yes
			game_start_spawn_artifacts_random_effect = yes
			game_start_spawn_artifacts_random_effect = yes
			game_start_spawn_artifacts_random_effect = yes
			game_start_spawn_artifacts_random_effect = yes
			game_start_spawn_artifacts_random_effect = yes
		}
	}
}