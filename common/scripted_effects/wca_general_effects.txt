artifact_craftsman_employer_clean_flags_variables_and_your_room_effect = { # Employer

	hidden_effect = {
		set_variable = { which = artifact_market_cost_variable value = 0 }
		clr_character_flag = bought_thing_recently
		clear_flags_with_prefix = bought_from_
		clear_flags_with_prefix = bought_artifact_

		if = {
			limit = { char_can_buy_artifacts_no_flag_potential_trigger = no }
			clr_character_flag = market_open
		}
	}

}

add_artifact_craftsman_cd_effect = {

	add_character_modifier = {
		name = employed_artifact_craftsman_recently
		hidden = yes
		years = 3
	}

}