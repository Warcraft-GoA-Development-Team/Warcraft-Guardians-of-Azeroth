decisions = {
	# Members of the Death God religion are allowed to join the Scourge
	host_gathering_of_clans_decision = {
		is_high_prio = yes
		
		ai_check_interval = 12

		only_playable = yes
		only_independent = yes

		potential = {
			independent = yes
			trait = creature_tauren
		}

		allow = {
			prestige = 2500 #temp
			# custom_tooltip = {
				# text = HIGHEST_PRESTIGE
				# any_character = {
					# limit = {
						# is_ruler = yes
						# OR = {
							# independent = yes
							# is_tributary = yes 
						# }
					# }
				# }
			# }
		}

		effect = {
			character_event = { id = WCTAR.100 }
		}

		revoke_allowed = {
			always = no
		}

		ai_will_do = {
			factor = 1
		}
	}
}